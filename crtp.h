/*
void initializeResources(
    int numResourceClasses, 
    int*numResourcesPerClass,
    int numTasks,
    int **maxResourcesPerClassPerTask
);



// and two run-time functions: 

void allocateResources(int resourceClass, int numResources);

void freeResources(int resourceClass, int numResources);
*/